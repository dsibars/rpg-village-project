// Re-export the actual Forge configuration from the infrastructure folder.
// Electron Forge expects this file at the project root.
export { default } from './infrastructure/electron/forge.config.js';
